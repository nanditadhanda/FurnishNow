import React from 'react'

const AuthorizationErrorScreen = () => {
    return (
        <div>
            You are not authorized to access this page.
            - if not logged in, log in to continue
            - go back to home button            
        </div>
    )
}

export default AuthorizationErrorScreen
